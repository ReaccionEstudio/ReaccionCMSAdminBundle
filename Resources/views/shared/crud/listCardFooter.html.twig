<div class="card-footer footer-pagination">
	<div class="row">
  		<div class="col-4">
    		<h3 class="card-title">Listing {{ getCurrentListingCount(result) }} of {{ result.getTotalItemCount }}</h3>
    	</div>
  		<div class="col-8 embed-pagination p-0">
    		{{ knp_pagination_render(result) }}
    	</div>
	</div>
</div>